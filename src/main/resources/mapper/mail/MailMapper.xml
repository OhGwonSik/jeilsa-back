<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeil.delivery.configuration.mail.mapper.MailMapper">
	<insert id="insertMailHistory" parameterType="com.jeil.delivery.configuration.mail.domain.MailHistoryVO">
		/* insertMailHistory */
		INSERT INTO mail_history (
			to_company_id, from_company_id, mail_status_cd, fail_log
			, send_dt, bill_id, bill_dtl_id
			, reg_id, chg_id, del_yn
		)
		VALUES (
			#{param.toCompanyId}, #{param.fromCompanyId}, #{param.mailStatusCd}, #{param.failLog}
			, now(), #{param.billId}, #{param.billDtlId}
			, #{memberInfo.memberId}, #{memberInfo.memberId}, 'N'
		)
	</insert>
</mapper>
